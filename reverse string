public class MyClass {
    public static void main(String args[]) {
      String s="system";
      s=s.toLowerCase();
      int n= s.length();
 String ns="";
 char ch;
 for(int i=0;i<n;i++){
     ch=s.charAt(i);
     ns =ch+ns;
 }
 System.out.println(ns);

    
    }
}
